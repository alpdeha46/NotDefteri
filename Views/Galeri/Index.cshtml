@model List<NotDefteri.Models.Resim>

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

<div class="container mt-5">
    <h2>📷 Galeri</h2>
<br>
    <a asp-action="Ekle" class="btn btn-success mb-4">Yeni Resim Ekle</a>

    @if (Model.Count == 0)
    {
        <p class="text-white">Henüz resim yok.</p>
    }
    else
    {
        <div class="row g-4">
            @foreach (var resim in Model)
            {
                <div class="col-12 col-sm-6 col-md-4 col-lg-3">
                    <div class="card h-100 shadow-sm">
                        <img src="@resim.ResimUrl" class="card-img-top" style="height:200px; object-fit:cover;" />
                        <div class="card-body d-flex flex-column">
                            <h5 class="card-title">@resim.Ad</h5>
                            <p class="card-text flex-grow-1">@resim.Aciklama</p>
                            <div class="d-flex justify-content-between mt-2">
                                <a asp-action="Duzenle" asp-route-id="@resim.Id" class="btn btn-primary btn-sm">Düzenle</a>
                                <a asp-action="Sil" asp-route-id="@resim.Id" class="btn btn-danger btn-sm"
                                   onclick="return confirm('Silmek istiyor musun?');">Sil</a>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
</div>