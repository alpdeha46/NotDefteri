@{
    var kullanici = Context.Session.GetString("kullanici");
}

<div class="container mt-5">
    <div class="card shadow-lg p-5">

        <h2 class="mb-4">⚙ Hesap Ayarları</h2>

        <!-- Kullanıcı Bilgisi -->
        <div class="mb-4">
            <h5>👤 Kullanıcı Bilgisi</h5>
            <p><strong>Kullanıcı Adı:</strong> @kullanici</p>
        </div>

        <hr />

        <!-- Şifre Değiştir -->
        <div class="mb-4">
            <h5>🔒 Şifre Değiştir</h5>

            <form asp-action="SifreDegistir" method="post">
                <div class="mb-2">
                    <input type="password" name="eskiSifre"
                           class="form-control" placeholder="Eski Şifre" required />
                </div>

                <div class="mb-2">
                    <input type="password" name="yeniSifre"
                           class="form-control" placeholder="Yeni Şifre" required />
                </div>

                <button class="btn btn-primary">
                    Güncelle
                </button>
            </form>
        </div>

        <hr />

        <!-- Not Ayarları -->
        <div class="mb-4">
            <h5>📝 Not Ayarları</h5>

            <div class="form-check">
                <input class="form-check-input" type="checkbox" />
                <label class="form-check-label">
                    Not oluştururken tarihi göster
                </label>
            </div>

            <div class="form-check mt-2">
                <input class="form-check-input" type="checkbox" />
                <label class="form-check-label">
                    Notları en yeniden eskiye sırala
                </label>
            </div>
        </div>

        <hr />

        <!-- Tehlikeli Alan -->
        <div class="mb-4">
            <h5 class="text-danger">🗑 Tehlikeli İşlemler</h5>

            <form asp-action="TumNotlariSil" method="post"
                  onsubmit="return confirm('Tüm notlar silinsin mi?');">
                <button class="btn btn-danger">
                    Tüm Notları Sil
                </button>
            </form>
        </div>

        <hr />

        <a asp-controller="Home" asp-action="Cikis"
           class="btn btn-dark w-100">
            Çıkış Yap
        </a>

    </div>
</div>